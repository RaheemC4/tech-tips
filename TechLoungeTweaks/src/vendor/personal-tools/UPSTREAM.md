# Supplied personal tools

The owner supplied these archives locally on 12 September 2026:

- IObit Driver Booster Pro v13.5.1.400 Portable Cracked {CracksHash}.rar
- TreeSize Professional v9.8.0.2301 (x64) Portable Cracked {CracksHash}.rar

Archive and executable SHA-256 values are recorded in `bundled-tools.json`.
Application files are copied from the respective `App/DriverBooster` and
`App/TreeSize` folders. No application executable is patched by TechLoungeTweaks.
These supplied files already have invalid publisher signatures.

`driverbooster.ini` is the original Driver Booster `App/DefaultData/DriverBooster/Config.ini`.
The integration preserves its manual-update, no-auto-scan, no-auto-download,
and exit-on-close settings. Other user preferences remain intact.

The outer portable launchers, registry-import wrappers, promotional shortcuts,
and encrypted `App/AppInfo/thanks4dl.rar` are not distributed. No Windows INF or
Tasks directories are relocated, and no global LicenseManager debugger is set.
