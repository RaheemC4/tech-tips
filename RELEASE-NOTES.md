Curated debloat, clearer Windows setup and reliable window closing.

- Added Debloat & Customization using pinned Win11Debloat helpers, with live status, explicit selections, registry backups and individual undo actions.
- Dashboard and tab Recommended buttons share the same privacy, optional-app removal and requested customization preset, preserving gaming features, Store and printing.
- Clearing existing Start pins runs once per profile and permits later repinning; Apply All requires confirmation.
- Already-activated PCs now show immediate feedback using the displayed status; setup actions clear stale messages and show loading indicators.
- Added an in-app mouse-operated edition chooser with current edition, supported targets and confirmation before a hidden helper applies the change.
- Fixed the title-bar X to close the actual WebView window.
- Restored cyan batch output and repository/download shortcuts while keeping GitHub publishing a single-step workflow.
- Updated README, screenshots, automated checks and the personal distribution ZIP.
- Fixed ZIP replacement failures with bounded retries and a backup-and-rename fallback for cloud-managed files; preserve both builds if access remains blocked.
